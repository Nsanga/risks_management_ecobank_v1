name: Deploy React App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    env:
      REACT_APP_API_URL: ${{ secrets.REACT_APP_API_URL }}

    steps:
      - name: Checkout du code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Installation des dépendances
        run: npm install

      - name: Build de l'application
        run: npm run build

      # - name: Déploiement sur le serveur
      #   uses: appleboy/ssh-action@v0.1.5
      #   with:
      #     host: ${{ secrets.SERVER_IP }}
      #     username: ${{ secrets.SERVER_USER }}
      #     key: ${{ secrets.SERVER_SSH_KEY }}
      #     port: 22
      #     script: |
      #       echo "Déploiement de l'application..."
      #       # Nettoyer l'ancien build (optionnel)
      #       rm -rf /var/www/frontend/risks_management_ecobank_v1/build/*
      #       # Copier le nouveau build sur le serveur
      #       cp -R ${{ github.workspace }}/build/* /var/www/frontend/risks_management_ecobank_v1/build/

